"""Shared code for the EuroScope ESE to VATGlasses conversion scripts.

- ``config``: repository paths and ``config/config.yml`` loading (needs PyYAML).
- ``ese``: EuroScope sector file parsing and ESE input selection.
- ``vatglasses``: VATGlasses JSON I/O and geometry helpers (standard library only).
"""
